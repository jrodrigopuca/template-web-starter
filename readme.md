Starter template

En este nuevo comienzo se elimina la dependencia de la gem middleware

Comando para configurar bundle
bundle config set path 'vendor/bundle' && bundle install

Para iniciar el watch
bundle exec guard
